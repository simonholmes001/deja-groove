namespace DejaGroove.Domain.Scanning;

public static class ConfidenceThreshold
{
    // Domain constant — owned by domain, not by any adapter or configuration
    public const float Minimum = 0.75f;
}

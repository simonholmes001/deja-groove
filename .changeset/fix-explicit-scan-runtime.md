---
"deja-groove": patch
---

Fix scan and crate flow regressions by making the scan runtime mode explicit in configuration, preserving fail-closed defaults, and keeping the deployed environment wired intentionally.
